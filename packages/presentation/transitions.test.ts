import {describe,expect,test} from 'bun:test';import sharp from 'sharp';import {TransitionCompiler} from './transitions';
const canvas=async(color:string)=>await sharp({create:{width:480,height:272,channels:4,background:color}}).png().toBuffer();
describe('TransitionCompiler',()=>{test('compiles bounded 15-key frames and exact destination',async()=>{const c=new TransitionCompiler(30);const plan=await c.compile(await canvas('#000000'),await canvas('#ffffff'),{type:'crossfade',durationMs:280},7);expect(plan.frames.length).toBeLessThanOrEqual(12);expect(plan.frames.at(-1)?.offsetMs).toBe(280);expect(plan.frames.every(f=>f.keys.length===15)).toBe(true);expect(plan.generation).toBe('7');});test('none produces just final frame',async()=>{expect((await new TransitionCompiler().compile(await canvas('red'),await canvas('blue'),{type:'none',durationMs:0},1)).frames).toHaveLength(1);});});
