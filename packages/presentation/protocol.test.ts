import {describe,expect,test} from 'bun:test';import {parsePluginMessage,parseRuntimeMessage} from './protocol';
const png='data:image/png;base64,iVBORw0KGgo=';
describe('plugin protocol',()=>{test('accepts bounded messages',()=>{expect(parsePluginMessage({v:1,type:'cells-ready',deviceId:'deck'})).toEqual({v:1,type:'cells-ready',deviceId:'deck'});expect(parsePluginMessage({v:1,type:'key',phase:'down',index:14,generation:'g'})).toMatchObject({index:14});expect(parsePluginMessage({v:1,type:'frame-sent',generation:'g',frame:0})).toMatchObject({frame:0});expect(parseRuntimeMessage({v:1,type:'presentation',trigger:'unlock',plan:{generation:'g',frames:[{index:0,offsetMs:0,keys:Array(15).fill(png)}]},inputEnabled:false})).toMatchObject({type:'presentation'});});test('rejects unknown, oversized and unsafe payloads',()=>{for(const bad of [{v:2,type:'cells-ready',deviceId:'x'},{v:1,type:'key',phase:'down',index:15,generation:'g'},{v:1,type:'cells-ready',deviceId:'x',extra:true}])expect(()=>parsePluginMessage(bad)).toThrow();expect(()=>parseRuntimeMessage({v:1,type:'presentation',trigger:'page',plan:{generation:'g',frames:[{index:0,offsetMs:0,keys:Array(15).fill('file:///x')}]},inputEnabled:true})).toThrow();});});
