import {expect,test} from 'bun:test';import {defaultStudioDocument} from '../../studio/document';import {StudioModel} from './model';
test('canvas model edits backgrounds, transparent apps, dynamic regions, motion and history',()=>{const model=new StudioModel(defaultStudioDocument());model.setBackground('page','a'.repeat(64));model.setButton({index:0,type:'app',bundleId:'org.mozilla.firefox',label:'Firefox',appearance:{opacity:0}});model.setDynamicRegion({id:'claude',keys:[1,2],signals:{source:'claude'},order:'recent',overflow:'paginate',empty:'background'});model.setMotion('unlock',{type:'fade-through-black',durationMs:400});expect(model.document.pages[0].buttons?.[0]).toMatchObject({type:'app'});model.undo();expect(model.document.motion.unlock.type).toBe('crossfade');model.redo();expect(model.dirty).toBe(true);model.markApplied();expect(model.dirty).toBe(false);});
